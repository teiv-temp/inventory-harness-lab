# 에이전트 작업 지침

질문별 원본을 먼저 읽고, 필요한 경우에만 탐색 범위를 넓힌다. 작업 시작 시 `docs/` 전체를 읽지 않는다.

## 라우팅

| 질문 | 먼저 읽을 원본 |
|---|---|
| 제품 요구사항·범위·완료 기준·재고 정책 | `docs/01-requirements.md` |
| 사용자·페르소나 | `docs/02-personas.md` |
| 업무 흐름·시나리오 | `docs/03-scenarios.md` |
| 차별화·참여 경험 | `docs/04-engagement.md` |
| UI·접근성·반응형 | `docs/05-design.md` |
| 기술·데이터·트랜잭션·아키텍처 | `docs/06-architecture.md` |
| 구현 계획·마일스톤·기존 QA 계획 | `docs/07-plan.md` |
| 현재 상태·미구현·다음 작업 | `docs/HANDOVER.md` |
| 설치·실행 명령 | `README.md` |
| 개별 작업의 범위·수용 기준·상태 | 해당 GitHub Issue |
| SSOT·원본 우선순위·보호 경계 | `docs/harness/ssot.md` |
| 최초 기준·증적·문서화 갭 | `docs/harness/ssot-appendix.md` (해당 정보가 필요할 때만) |
| 검증 규칙·구현/검증 루프 | 현재 없음 — 추후 생성 |
| 운영·배포·백업·복구 | 지정 원본 없음 — 추론 금지 |

## 읽기 순서

1. 현재 경로부터 루트까지 `CLAUDE.md`와 `AGENTS.md`를 확인한다. 더 가까운 지침을 적용하되 원본·무결성 보호 규칙은 유지한다.
2. 요청을 유형별로 분류하고 위 표의 원본만 읽는다.
3. 원본만으로 답할 수 있으면 여기서 멈춘다.
4. 판단할 수 없을 때만 관련 보조 문서, 현재 상태, Issue/PR, 코드·스키마·테스트 순서로 최소 확장한다.
5. 확장한 이유와 실제로 읽은 자료를 결과에 남긴다.

## 원본·보호 규칙

- 재고 도메인은 `docs/01-requirements.md`, 아키텍처는 `docs/06-architecture.md`, 작업 계약은 해당 GitHub Issue를 따른다.
- 참고 문서가 SSOT와 충돌하면 SSOT를 우선한다.
- Issue와 SSOT가 충돌하거나 SSOT와 SSOT가 충돌하면 `NEEDS_HUMAN`을 선언하고 사람에게 판단을 요청한다.
- 충돌한 상태에서 AI는 자신의 판단대로 구현·문서 수정·데이터 변경·완료 선언을 하지 않는다.
- 코드 주석·현재 구현·PR·커밋·스크린샷·파생 문서는 지정 원본을 덮어쓰지 못한다.
- 요구사항과 현재 구현이 다르면 `기준상 기대`와 `현재 동작`을 분리해 보고한다.
- 원본이 없으면 임의로 선택·추론하지 말고 `미정`, `충돌`, `확인 필요`로 보고한다.
- Issue가 없으면 `Issue 미지정`으로 표시한다. PR이나 커밋을 Issue로 간주하지 않는다.
- 검증 규칙과 구현·검증 루프가 추후 생성 상태인 동안 임시 규칙을 공식 기준처럼 만들지 않는다.

## 결과 기록

관련 결과에는 실제로 읽은 자료, 판단 기준, 결론, 확장 사유(있을 때), 충돌 여부, 미확정 사항을 간단히 적는다.

상세 원본 매핑과 보호 경계는 [`docs/harness/ssot.md`](docs/harness/ssot.md)를 참조한다. 단, 모든 질문에서 이 문서를 먼저 읽지는 말고 SSOT·우선순위·충돌 판단이 필요한 경우에만 읽는다. 최초 기준·증적·갭 인벤토리는 [`docs/harness/ssot-appendix.md`](docs/harness/ssot-appendix.md)를 해당 정보가 필요할 때만 읽는다.
